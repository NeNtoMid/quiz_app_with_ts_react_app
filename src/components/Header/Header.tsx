import React from 'react';

import * as Styles from './HeaderStyle';

const Header = () => {
	return (
		<Styles.Header>
			<h1>React Quiz</h1>
			<button>Start</button>
		</Styles.Header>
	);
};

export default Header;
